{
  "name": "openassistant-desktop-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright UI tests for the openAssistant desktop frontend (browser mode, mock backend).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
